I have already created my website, and now I want to add a **form section** to it.

Please create the form using **only HTML and CSS**. I want to be able to **copy and paste the code directly into my existing project**.

Requirements:

- Give me the **HTML and CSS separately**.
- Do not include JavaScript or any external libraries/frameworks.
- Make the form **clean, modern, and beginner-friendly**.
- Make it **responsive** so it works well on desktop and mobile.
- Use **unique class names** for every element (for example, prefix all classes with `admission-form-`) so they do not conflict with the existing CSS classes on my website.
- Do not modify or overwrite common/global selectors such as `body`, `h1`, `p`, `form`, `input`, or `button`.
- Keep the CSS **self-contained** and apply styles only to this form section.
- Make sure the HTML and CSS work together without requiring additional changes.
- Add clear comments where necessary so I can understand what each section does.

First, provide the complete HTML code, then provide the complete CSS code.